{
  "rules": {
    ".read": "auth != null",  // 인증된 사용자만 읽기 가능
    "test_results": {
      ".read": true,  // 테스트 결과는 누구나 읽을 수 있음 (관리자 페이지용)
      ".write": true   // 테스트 결과는 누구나 쓸 수 있음 (테스트 결과 저장용)
    }
  }
}
